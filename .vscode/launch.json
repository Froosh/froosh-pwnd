{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "node-terminal",
            "name": "Start Function",
            "request": "launch",
            "preLaunchTask": "npm install",
            "command": "npm run start",
            "cwd": "${workspaceFolder}"
        }
    ]
}
